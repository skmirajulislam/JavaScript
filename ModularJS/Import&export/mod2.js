let obj = { pname: 'Mobile', price: 10 };

let a = 20;
let b = 30;

function add(i, j) {
    return i + j;
}
export {obj, add }; // exporting the data 
export default a; // single anonymous export we dont need to name it